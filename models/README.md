# models/

Place GGUF model files here for local AI interpretation.

Run the download script to fetch a model automatically:

    python download_model.py

Or download manually from HuggingFace and drop the .gguf file into this folder.
The app will detect it automatically in the sidebar → AI Settings.
